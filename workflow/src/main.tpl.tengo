wf := import("@platforma-sdk/workflow-tengo:workflow")
assets:= import("@platforma-sdk/workflow-tengo:assets")
render := import("@platforma-sdk/workflow-tengo:render")
pframes := import("@platforma-sdk/workflow-tengo:pframes")

processTpl := assets.importTemplate(":process")


wf.body(func(args) {
	input := wf.resolve(args.input)

	receptorsOrChains := ["IG", "TCRAB", "TCRGD"]
	if !is_undefined(args.chains) {
		receptorsOrChains = args.chains
	}

	runMixcr := render.createEphemeral(processTpl, {
		inputSpec: input.getFutureInputField("spec"),
		inputData: input.getFutureInputField("data"),
		preset: args.preset,
		blockId: wf.blockId().getDataAsJson(),
		limitInput: args.limitInput,
		receptorsOrChains: receptorsOrChains
	})

	// The VBC/clonotype pipeline result (mixcr export -> VBC filter/normalize -> aggregate).
	clonotypes := runMixcr.output("clonotypes")

	exports := {
		clones: clonotypes,
		clns: {
			spec: runMixcr.output("clns.spec"),
			data: runMixcr.output("clns.data")
		}
	}

	return {
		outputs: {
			logs: runMixcr.output("logs.data"),
			qc: pframes.exportColumnData(runMixcr.output("qc.data")),
			reports: pframes.exportColumnData(runMixcr.output("reports.data")),
			// `clones` is surfaced in outputs (in addition to exports, which feeds downstream
			// blocks) so the model's isRunning waits on the VBC pipeline (not just the MiXCR
			// analyze outputs). The model reads only ctx.outputs, never exports. A p-frame in
			// outputs must be wrapped with exportFrame (a raw frame is only valid in exports);
			// cf. mixcr-amplicon-alignment / mixcr-scfv-clonotyping. NOTE: getError() on this
			// frame does not surface per-chain sub-resource errors — a failed VBC step is
			// instead detected via `vbcStatus` below.
			clones: pframes.exportFrame(clonotypes),

			// Per-chain VBC step status (a nested ResourceMap: [chain] -> per-sample
			// cloneTableTsv ResourceMap), surfaced raw like `logs`. The model walks both
			// levels and detects a failed VBC Python step that the clones exportFrame hides.
			vbcStatus: runMixcr.output("vbcStatus")
		},
		exports: exports
	}
})
